# 實作狀態

更新：2026-09-11。本機回歸 48 項程式測試、10 項瀏覽器流程、重啟／刪除驗收與 8 輪檔案並行回歸通過。這不等同一學期所有驗收已達標。

| 項目           | 已完成                                                                                | 尚需完成                                                                                    |
| -------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 網站與架構     | React、TypeScript、Vite、Hono、共用 Zod 型別、PWA 外殼、單一服務靜態檔                | 實機 PWA 安裝、正式手機可用性測試                                                           |
| 登入           | Firebase Google／密碼、註冊、驗證信、重設密碼、明確連結登入方式、UID 隔離             | 設定 Firebase 專案；實際兩帳號兩裝置、連結衝突、信箱驗證與停用測試                          |
| 資料管理       | 個人資料、健康問卷、藥品 CRUD、草稿、歷史紀錄、更正、匯出、刪除佇列                   | Firestore 真實環境整合與帳號刪除故障演練                                                    |
| 官方主檔       | 66,478 筆本機資料、版本化索引、候選排序、每週更新端點與排程指令                       | 將主檔匯入雲端並驗證週更新                                                                  |
| 官方仿單       | 30 份官方 PDF；2 份逐頁文字核對後供本機測試，28 份草稿；後台發布與版本淘汰           | 其餘逐頁來源核對、專業審閱；尚未完成 30–50 份正式發布                                      |
| NVIDIA         | 七種合成影像、完整官方仿單／測驗畫面、實際 API 重試、兩藥引用範圍實測                | 最新完整瀏覽器問答遭逾時，重試保守回覆資料不足；模型穩定性、真實照片及醫療品質仍待驗證       |
| 附來源 AI      | 引用歸屬／字串驗證、獨立語意審核、白話說明、警語、問答、兩藥交互作用、測驗、個人快取  | 專家標註的 100 題醫療問答；模型審核不能保證結論正確                                         |
| 排程與推播     | 台北時區、自選時間、PRN 分流、打卡冪等、通知 outbox、FCM 裝置管理、失效 token 清除    | Cloud Scheduler／Tasks OIDC 實測、Android 背景推播與失效 token 整合測試                     |
| 家庭藥箱與照護 | 存量／效期／處理狀態、一次性邀請、唯讀授權、撤銷、前景刷新、60 分鐘未記錄通知         | 真實家人裝置通知、可用性研究                                                                |
| 後台           | 帳號搜尋、停用／恢復、50 名額交易、仿單、任務重試、用量、稽核 API                     | 首位雲端管理員設定、運維錯誤告警接收者                                                      |
| 基礎設施       | Docker、多環境部署腳本、私有 worker、Secret Manager、7 天備份排程、還原腳本、預算範例 | 專案 ID／Firebase web config／VAPID／Cloud 登入；尚未部署、未設實際費用通知、未執行還原演練 |
| 專題評估       | 50 張合成圖片、100 題來源檢索草稿、切分標記、指標工具                                 | 50 張取得同意且去識別化的真實照片；正式問題與人工答案；排名及直接生成對照實驗               |

## 設計上保留的限制

- 所有生成快取目前都按使用者隔離；尚未另做跨使用者的共用白話內容快取，避免個人健康資料誤共用。
- 掃描仿單目前支援人工補正文字；未提供後台影像模型 OCR 輔助按鈕。
- 訪問健康頁面從雲端 API 讀取；不實作離線健康資料與離線同步佇列。Firebase 登入使用記憶體持續性，重整頁面可能需要重新登入。
- 多模型分工已改為 NVIDIA 文字模型負責生成及驗證。兩次模型判斷並不是獨立的醫療專業審查。
- 通知採 lease 與相同 tag 去重；FCM 成功後、儲存送達狀態前若程序崩潰，可能重送。不能宣稱外部通知絕對 exactly-once。
- 晚到一天以上的家人提醒不補發；目前檢查台北當日到期紀錄。
- 測試／正式使用兩個獨立 Firebase 專案。腳本每個 Cloud Run 服務最多 2 個執行個體；web 和 worker 的上限分別計算。
- 每人／全站額度限制的是每日 AI **任務建立次數**（20／200），一次任務可能有生成、驗證和有限 HTTP 重試。這不是實際金額或 token 保證。

## 下一個可驗收階段

依目前決定先完成本機，Google Cloud 延後。本機可用 `npm run check:local` 驗收，操作見 `docs/LOCAL-TESTING.md`。正式交付仍需補足仿單逐頁審查、真實影像與問答標註及實機測試；日後有 Firebase 設定再驗證真實登入、私有 worker、FCM 與還原演練。
