rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application data goes through the authenticated Hono API.
    match /{document=**} { allow read, write: if false; }
  }
}
